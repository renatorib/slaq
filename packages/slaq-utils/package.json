{
  "name": "slaq-utils",
  "version": "0.3.0",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "build": "yarn clean && babel src --out-dir lib --extensions '.js,.ts' && tsc",
    "watch": "watch -p 'src/**/*.{ts,js}' -c 'yarn build'"
  },
  "dependencies": {
    "debug": "^4.1.1"
  }
}
