{
  "name": "slaq-options",
  "version": "0.3.0",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rm -rf ./lib",
    "build": "yarn clean && babel src -d lib -x '.js,.ts' --ignore '**/*.test.ts' && tsc",
    "watch": "watch -p 'src/**/*.{ts,js}' -c 'yarn build'",
    "test:unit": "jest"
  },
  "dependencies": {
    "@types/debug": "^4.1.5",
    "debug": "^4.1.1",
    "slaq-utils": "^0.3.0"
  },
  "peerDependencies": {
    "slaq": "*"
  }
}
